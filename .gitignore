# OS / editor junk
.DS_Store
.vscode/
.idea/
*.swp

# Python (carry-overs from the pre-restructure layout; the canonical
# Python project lives under py-1d/ which has its own .gitignore)
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
.coverage
.hypothesis/
.pytest_cache/

# Julia
/deps/build.log
/docs/build/
/docs/site/
*.cov
.julia/
LocalPreferences.toml

# Generated outputs (large simulation artifacts; small ones can be
# tracked under reference/ explicitly)
*.h5
*.hdf5
*.jld2
*.vtu
*.vti
*.vtp
*.pvd

# Tier-A regression goldens are tracked when ≤ 5 MiB (see
# reference/golden/SCHEMA.md "Size policy"). The matching .h5.regen
# stubs are tracked unconditionally and tell CI how to rebuild a
# golden whose .h5 was suppressed by the size budget.
!reference/golden/*.h5
!reference/golden/*.h5.regen

# Generated paper artifacts (top-level; py-1d has its own copy)
paper/*.aux
paper/*.log
paper/*.out
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
